version: "3.9"

services:
  alice:
    build:
      context: .
      dockerfile: ./dockerfiles/actor.Dockerfile
    environment:
      PLAYER: alice
      PLAYERS: bob:4040
      IS_PLAYING: true

  bob:
    build:
      context: .
      dockerfile: ./dockerfiles/actor.Dockerfile
    environment:
      PLAYER: bob
      PLAYERS: alice:4040
